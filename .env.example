# MongoDB connection string
# Local:
MONGODB_URI=mongodb://localhost:27017/healthtrack
# MongoDB Atlas:
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/healthtrack?retryWrites=true&w=majority

# JWT secret key (change this to a strong random value in production)
JWT_SECRET=healthtrack-secret-key-change-this-in-production

# Server port
PORT=5000

# Node environment
NODE_ENV=development 